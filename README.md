# Network-Scanner-Toolkit
This project contains three Python programs to scan networks. It checks whether devices are active (ping), shows connected devices with MAC addresses (ARP), and performs advanced scanning using Nmap.
